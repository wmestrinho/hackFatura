# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Dev Server

```bash
python3 server.py 5500
```

Serves the app at `http://localhost:5500` with no-cache headers. The app is pure static HTML/CSS/JS — no build step, no npm, no bundler.

## Deployment

Push to `main` → **Cloudflare Pages** auto-deploys. Live at `https://mech.robotfantome.com`.

- GitHub Actions is **disabled** account-wide on this repo — Cloudflare Pages is the active deploy pipeline
- `robotfantome.com` nameservers are managed by Cloudflare
- GitHub Pages is configured but not actively building

## Version Bumping (required — see `../PROJECT-RULES.md`)

Single source of truth: `PCJ.version` in `js/config.js`. It populates the header badge, menu header, and footer automatically via `init()`. **Bump in the same commit as the change — never after.**

| Change | Bump |
|--------|------|
| Bug fix, style tweak | PATCH (e.g. v2.4.1 → v2.4.2) |
| New feature | MINOR (e.g. v2.4.x → v2.5.0) |

Also update the version table in `/Users/wmestrinho/.openclaw/workspace/PROJECT-RULES.md`.

## Architecture

Single-page application. All views live in `index.html` as `<div class="view">` elements. Navigation is `go(viewId)` in `js/app.js`, which toggles the `active` class.

**JS load order matters** (all via `<script>` tags in `index.html`):
1. `js/config.js` — `PCJ` config object + `STATE` (in-memory, backed by localStorage) + persistence helpers
2. `js/sheets.js` — Google Sheets API layer
3. `js/invoice.js` — jsPDF invoice generator (client-side only)
4. `js/app.js` — all UI logic, form handlers, dashboard, invoice dual mode, CSV export, entry delete/toggle

**Data flow:**
- Every form submit: saves to `STATE.localEntries` (localStorage) first, then fires-and-forgets to Google Sheets
- Dashboard reads exclusively from `STATE.localEntries` — fully offline
- Sheets writes are GET requests to work around CORS/redirect limitations; responses are opaque from the browser

**Views:** `mainMenu`, `tableWork`, `partsWork`, `invoiceBuilder`, `newCustomer`, `workCosts`, `eventDashboard`

## localStorage Keys

| Key | Contents |
|-----|----------|
| `pcj_user` | current operator name |
| `pcj_event` | current event name |
| `pcj_events` | event list array |
| `pcj_local_entries` | all submissions — `{type, event, org, amount, loggedBy, paymentStatus, savedAt, [serviceName], [invoiceNumber]}` |
| `pcj_local_customers` | org name list for datalist autocomplete |

Entry types: `'table'`, `'parts'`, `'cost'`, `'invoice'`

## Key Patterns

**Adding a new form type:**
1. Add a view in `index.html`
2. Add a `submit*` handler in `js/app.js` that calls `saveLocalEntry({ type: '...', ... })`
3. Add an appender function in `google-apps-script/Code.gs`
4. Register the action in `doGet`/`doPost` handlers in `Code.gs`

**Pricing:** centralized in `PCJ.pricing` in `js/config.js`. Service selects use `value="ServiceName|price"` format — split on `|` to extract both.

**Payment status values:** `'PAID'`, `'PENDING'`, `'INVOICED'` — always uppercase, stored as-is from form `<option value="">`.

**Invoice number format:** `PCJ-YYYY-MMDD-001` — generated by `generateInvoiceNumber()` in `app.js`, auto-increments based on today's saved invoices in `STATE.localEntries`.

**Entry identification:** entries use `savedAt` (ISO timestamp) as a unique key for delete/edit operations in the drill-down panel.

## Google Apps Script Backend

`google-apps-script/Code.gs` is the authoritative source. To deploy changes:

1. Copy the full file contents
2. Go to [script.google.com](https://script.google.com), open the project, paste and replace all
3. Create a **New Deployment** (always new — never redeploy existing)
4. Copy the new URL into `PCJ.SHEETS_SCRIPT_URL` in `js/config.js`

Spreadsheet ID: `1_I9kaE7ag1aULPneCB8kwuNksjWzI-EIrkTNXUp_VTQ`

Each event gets four tabs: `{Event}_TableWork`, `{Event}_PartsWork`, `{Event}_WorkCosts`, `{Event}_Invoices`. The `Customers` tab is global.

## Assets

Only `assets/pcj-logo.png` is tracked in git. Everything else in `assets/` is gitignored (marketing images, videos, PDFs — kept locally but not deployed). Do not add other files to `assets/` without updating `.gitignore`.
